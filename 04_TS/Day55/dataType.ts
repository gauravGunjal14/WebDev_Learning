// non-primitive data types in TypeScript
let arr:(string | number)[] = ['hello', 42, 'world', 100];

let tuple: [string, number, boolean] = ['TypeScript', 3.9, true];

let obj2: { name: string, version: number, isStable: boolean } = {
    name: 'TypeScript',
    version: 3.9,
    isStable: true
};